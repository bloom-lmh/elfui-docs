# Route view

The route view renders the component matched by the current location. Nested route views let parent layouts remain mounted while a child route changes.

Use nested views for layouts that own shared navigation, data boundaries, or visual chrome.
