# Route configuration

Route records map URL patterns to components and optional metadata. Give routes stable names when application code needs to navigate programmatically or attach policies such as titles and permissions.

Keep route-specific concerns in route metadata rather than scattering path checks through components.
