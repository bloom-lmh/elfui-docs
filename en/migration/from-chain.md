# Migrating from Chain to components

Replace Chain-oriented declarations with explicit components, props, events, and slots. Start at the outer component boundary, then move shared behavior into composables as needed.

Keep the old and new implementations side by side only long enough to verify equivalent behavior.
