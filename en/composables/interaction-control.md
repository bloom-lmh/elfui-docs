# Interaction control

Interaction-control composables manage common concerns such as focus, keyboard input, pointer events, and outside clicks. They centralize cleanup and edge cases that would otherwise be duplicated across components.

Use them to make interaction behavior explicit and testable.
