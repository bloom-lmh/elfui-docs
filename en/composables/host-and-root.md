# Host and root node

The host is the custom element that owns a component, while the root node is the rendered root for DOM operations. Use the appropriate reference when integrating with DOM APIs, shadow roots, or element attributes.

Do not assume the host and the rendered root are the same node.
