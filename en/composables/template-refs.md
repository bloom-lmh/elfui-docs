# Template refs

Template refs provide access to a rendered element or child component after mount. They are useful for focus management, measurements, and small imperative integrations.

Guard against a missing ref because the target can be conditionally rendered.
