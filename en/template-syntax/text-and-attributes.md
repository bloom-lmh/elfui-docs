# Text and attributes

Use interpolation to render text from state and bindings to keep element attributes in sync with state. Attribute bindings are appropriate for values such as `disabled`, `aria-*`, `href`, and custom element properties.

Use a computed value when an attribute needs formatting or conditional logic.
