# Class and style

Bind classes and inline styles from state for small, component-specific visual changes. Prefer semantic class names and stylesheet rules for reusable design decisions.

Use object or array bindings to compose conditional classes without manually concatenating strings.
