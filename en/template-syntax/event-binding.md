# Event binding

Bind event handlers in the template to respond to browser and custom events. Keep handlers focused on the event boundary, then call component methods or composables for the actual work.

Event modifiers can express common propagation and default-prevention behavior declaratively.
