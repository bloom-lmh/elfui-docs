# Form binding

Form bindings keep controls and component state synchronized. Use `v-model` for text fields, checkboxes, radios, and select controls when the default value and update event match your intent.

Normalize or validate data at the component boundary when a control needs a different internal representation.
