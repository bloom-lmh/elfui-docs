# Compiler diagnostics

Compiler diagnostics identify invalid template syntax, unsupported constructs, and likely mistakes before the application runs. Treat diagnostics as actionable feedback and fix the source location they identify.

Keep compiler and editor tooling versions aligned to receive the most accurate messages.
