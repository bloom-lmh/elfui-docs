# Transition

`Transition` applies enter and leave classes around a single element or component. Define the visual timing in CSS and let the component describe when the content appears or disappears.

Keep transition effects accessible by respecting reduced-motion preferences.
