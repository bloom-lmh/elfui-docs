# Local components

Register a component locally when it is used by one feature or view. Local registration keeps dependencies visible and prevents a component from becoming application-wide by accident.

Move a component to a shared module only when several features genuinely need it.
