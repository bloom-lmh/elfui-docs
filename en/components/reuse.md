# Component reuse

Reuse behavior through composables and reuse structure through components and slots. Prefer clear props and slots over a growing collection of boolean flags that try to cover unrelated layouts.

Composition keeps each reuse boundary small and testable.
