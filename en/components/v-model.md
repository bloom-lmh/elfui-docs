# v-model

`v-model` connects a component value with state in its parent. Define the value prop and the corresponding update event so consumers can use two-way binding without manual event wiring.

Use named models when a component exposes more than one editable value.
