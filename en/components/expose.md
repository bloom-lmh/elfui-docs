# Component expose

Expose a small, deliberate imperative API when a parent must call a child method or read a child value. Template refs should complement declarative props and events, not replace them.

Only expose behavior that is meaningful to a component consumer.
