# Custom directives

Create a custom directive for focused, reusable DOM behavior that does not belong in a component. A directive can react when its host element is mounted, updated, or removed.

Keep directives narrow: components should still own rendering and application state.
