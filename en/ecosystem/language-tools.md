---
title: Language Tools
---

# Language Tools

ElfUI language tools bring macro-component awareness into VS Code: completion, diagnostics, and support for the template syntax used in TypeScript component files.

Install the ElfUI VS Code extension from the editor marketplace, then open an ElfUI project configured with `@elfui/vite-plugin`. The language service uses the project configuration to keep diagnostics aligned with the compiler.
