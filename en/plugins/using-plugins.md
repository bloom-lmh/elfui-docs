# Using plugins

Install a plugin through the application setup API, then configure it with the documented options. Keep plugin setup centralized so each environment uses a predictable set of capabilities.

Prefer local components and composables for feature-specific behavior.
