# Shadow DOM

Shadow DOM encapsulates a custom element's markup and styles. It prevents most outside selectors from leaking in and keeps component internals private by default.

Expose deliberate styling hooks with CSS custom properties and parts when consumers need controlled customization.
