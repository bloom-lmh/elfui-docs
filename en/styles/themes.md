# Themes

Themes provide a controlled set of visual tokens such as colors, spacing, and typography. Define tokens once, then consume them in component styles so a theme can change without rewriting component rules.

Prefer semantic token names such as `surface` and `text-muted` over raw color names.
