---
title: 路由守卫
---

# 路由守卫

守卫可返回 `false` 取消导航、返回路径或命名 location 重定向，也可返回 Promise。

```ts
router.beforeEach((to) => {
  if (to.meta.requiresAuth && !auth.loggedIn.value) return "/login";
});
router.beforeResolve((to) => preloadPageData(to));
router.afterEach((to, from, failure) => {
  if (!failure) analytics.page(to.fullPath, from.fullPath);
});
router.onError((error) => reportError(error));
```

异步路由组件会在 `beforeResolve` 前加载；重定向循环会交给 `onError`。

```ts
onBeforeRouteLeave(() => !dirty.value || window.confirm("放弃修改？"));
onBeforeRouteUpdate((to) => loadUser(to.params.id));
```

执行顺序为：最深层 leave 守卫、全局 `beforeEach`、组件 update 守卫、路由记录 `beforeEnter`、异步组件加载、`beforeResolve`。ElfUI 页面是 Custom Element，因此只提供 leave/update 组件守卫，不提供 Vue Options API 风格的 `beforeRouteEnter` 实例回调。
