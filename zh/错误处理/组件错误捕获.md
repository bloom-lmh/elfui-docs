---
title: 组件错误捕获
---

# 组件错误捕获

`onErrorCaptured()` 捕获子组件冒泡上来的错误。

```ts{1-4}
onErrorCaptured((err) => {
  console.error(err);
  return false;
});
```

返回 `false` 表示阻止继续向上冒泡。

## 使用场景

- 局部错误提示
- 子组件失败后展示 fallback
- 阻止错误污染整页

如果需要完整的 fallback/retry 结构，可以使用错误边界。
